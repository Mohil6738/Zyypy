info:
  name: Driver Lists
  type: http
  seq: 32

http:
  method: GET
  url: "{{base_url}}driver/list"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
