info:
  name: FetchNearbyMerchantLocations
  type: http
  seq: 15

http:
  method: GET
  url: "{{base_url}}driver/fetch-mearby-merchantlocations"
  body:
    type: json
    data: |-
      {
          "fe_code": "CL_PAN",
          "latitude": "25.24792563",
          "longitude": "51.55262500"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
