info:
  name: Fetch ZOnes
  type: http
  seq: 23

http:
  method: GET
  url: "{{base_url}}zones"
  body:
    type: json
    data: get_location/{zone}/{street}/{building}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
