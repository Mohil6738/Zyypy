info:
  name: FetchDashboardDetails
  type: http
  seq: 11

http:
  method: GET
  url: "{{base_url}}driver/fetch-dashboard-details?fe_code=CL_PAN"
  params:
    - name: fe_code
      value: CL_PAN
      type: query
  body:
    type: json
    data: ""
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
