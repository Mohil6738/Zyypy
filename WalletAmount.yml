info:
  name: WalletAmount
  type: http
  seq: 33

http:
  method: GET
  url: "{{base_url}}driver/get-wallet-amount"
  body:
    type: json
    data: |-
      {
          "fe_code":"FED_USP"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
