info:
  name: fetch latitude longitude
  type: http
  seq: 28

http:
  method: GET
  url: "{{base_url}}latitudelogitude/1/119/20"
  body:
    type: json
    data: get_location/{zone}/{street}/{building}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
