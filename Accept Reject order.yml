info:
  name: Accept Reject order
  type: http
  seq: 20

http:
  method: POST
  url: "{{base_url}}driver/accept-reject-order"
  body:
    type: json
    data: |
      {
          "awb_no":"CL_2026020901",
          "status":"REJECTED",
          "fe_code":"CL_VK"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
