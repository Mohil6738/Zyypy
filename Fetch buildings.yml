info:
  name: Fetch buildings
  type: http
  seq: 25

http:
  method: GET
  url: "{{base_url}}buildings/1/119"
  body:
    type: json
    data: get_location/{zone}/{street}/{building}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
