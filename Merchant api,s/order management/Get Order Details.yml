info:
  name: Get Order Details
  type: http
  seq: 3

http:
  method: GET
  url: "{{base_url}}mobile-merchant/order/CL_2026042801?merchant_id=2&merchant_type=C2C"
  headers:
    - name: Accept
      value: application/json
  params:
    - name: merchant_id
      value: "2"
      type: query
    - name: merchant_type
      value: C2C
      type: query
  body:
    type: json
    data: ""
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
