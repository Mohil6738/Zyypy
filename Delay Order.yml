info:
  name: Delay Order
  type: http
  seq: 30

http:
  method: GET
  url: "{{base_url}}warehouse/manager/delayorders"
  body:
    type: json
    data: |-
      {
          "code":"CL_PANKAJ"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
