info:
  name: FetchManagerDashboardDetails
  type: http
  seq: 14

http:
  method: GET
  url: "{{base_url}}warehouse/manager/fetch-dashboard-details"
  body:
    type: json
    data: ""
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
